# SYSTEM 101 - 1 : Container is Isolated

Workshop นี้มีวัตถุประสงค์เพื่อให้เข้าใจว่า **Container มี filesystem แยกจาก Host**

---

## Objective

- เข้าใจ concept ของ Container Isolation
- ทดลองแก้ไขไฟล์ภายใน container โดยตรง
- เห็นผลลัพธ์เมื่อ container ถูก Stop และ Remove
- เปลี่ยนจากการใช้ `docker run` เป็น `docker-compose.yml`

---

## Step 1 : Run NGINX Container

```bash
docker run -d --name nginx-lab -p 8080:80 nginx:alpine
```

เปิด Browser:
http://localhost:8080

---

## Step 2 : เข้าไปแก้ไขไฟล์ใน Container

```bash
docker exec -it nginx-lab sh
cd /usr/share/nginx/html
echo "Hello from inside container" > index.html
```

Refresh หน้าเว็บ จะเห็นผลลัพธ์เปลี่ยนทันที

---

## Step 3 : Stop และ Remove Container

```bash
docker stop nginx-lab && docker rm nginx-lab
docker run -d --name nginx-lab -p 8080:80 nginx:alpine
```

ไฟล์จะกลับเป็นค่าเดิม

---

## 🚧 TASK-01 : Docker Compose

> 🎯 เป้าหมาย  
> ทำให้ได้ผลลัพธ์เหมือนเดิม  
> แต่เปลี่ยนวิธีรันจาก `docker run` → **Docker Compose**

---

> 💡 คำใบ้  
> - โครงสร้างไฟล์ Compose เป็นแบบลำดับชั้น  
> - port ฝั่งซ้ายคือ Host, ฝั่งขวาคือ Container  
> - service name ไม่จำเป็นต้องเท่ากับ container name

---

### การทดสอบ

- ต้องมีคำสั่งสำหรับ **เริ่ม container**
- ต้องมีคำสั่งสำหรับ **หยุดและลบ container ทั้งหมด**

> 💡 คำใบ้  
> คำสั่งทั้งสองเป็นคำสั่งคู่กัน และอ้างอิงไฟล์เดียวกัน

---

### จุดสังเกต

- หลังจาก container ถูกลบ แล้วรันใหม่
- ไฟล์ HTML ที่เคยแก้ไขไว้ยังอยู่หรือไม่
- สิ่งนี้บอกอะไรเกี่ยวกับธรรมชาติของ container

---

## Conclusion

- Container ถูกออกแบบให้ **Isolated**
- ข้อมูลภายใน container จะไม่ Persist หากไม่มี Volume
- Docker Compose ช่วยจัดการ container ให้เป็นระบบมากขึ้น

---

**Next Lab:**  
SYSTEM 101 - 2 : Container + Volume
