# SYSTEM 101 - 1 : Container is Isolated

Workshop นี้ออกแบบมาเพื่อให้เข้าใจพื้นฐานที่สำคัญที่สุดของ Docker คือ  
**Container มีการแยกตัว (Isolation) ออกจาก Host อย่างชัดเจน**

---

## Objective

- เข้าใจว่า Container มี filesystem ของตัวเอง
- เห็นผลของการแก้ไขไฟล์ภายใน container
- สังเกตสิ่งที่เกิดขึ้นเมื่อ container ถูกลบ
- เริ่มต้นใช้งาน Docker Compose

---

## Lab Overview

ใน Lab นี้จะใช้ **NGINX Container** เป็นตัวอย่าง  
เพื่อให้เห็นภาพการทำงานของ container ได้ชัดเจนที่สุด

---

## TASK-01 : Docker Compose (Hint-based)

> 🎯 เป้าหมาย  
> เปลี่ยนจากการรัน container แบบ `docker run`  
> มาเป็นการจัดการด้วย **Docker Compose**

---

### สิ่งที่ต้องทำ

- สร้างไฟล์ชื่อ `docker-compose.yml`
- ภายในไฟล์ต้องมีอย่างน้อย 1 service
- ใช้ image เดียวกับที่เคยใช้ใน Lab ก่อนหน้า
- ต้องสามารถเข้าเว็บผ่าน `localhost:8080` ได้

> 💡 คำใบ้  
> - image ควรเป็น image ขนาดเล็ก  
> - port ฝั่งซ้ายคือ Host, ฝั่งขวาคือ Container  
> - service name ไม่มีผลกับชื่อ container เสมอไป

---

### การทดสอบ

- ใช้คำสั่งเดียวเพื่อ **เริ่ม container**
- ใช้คำสั่งเดียวเพื่อ **หยุดและลบ container ทั้งหมด**

> 💡 คำใบ้  
> คำสั่งทั้งสองเป็นคำสั่งคู่กัน และใช้ไฟล์เดียวกัน

---

### สิ่งที่ต้องสังเกต

- หลังจาก stop และ run ใหม่
- ไฟล์ที่เคยแก้ไขภายใน container ยังอยู่หรือไม่
- พฤติกรรมนี้สะท้อนอะไรเกี่ยวกับ container

---

## Conclusion

- Container ถูกออกแบบให้ **Isolated และ Disposable**
- ข้อมูลภายใน container จะไม่ Persist หากไม่มี Volume
- Docker Compose ช่วยให้การจัดการ container เป็นระบบและอ่านง่ายขึ้น

---

**Next Lab:**  
SYSTEM 101 - 2 : Container + Volume
